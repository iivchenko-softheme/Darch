<UserControl x:Class="Darch.GUI.Wpf.Views.StateControlView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d">
    <WrapPanel>
        <Label Content="Name:"/>
        <Label Content="{Binding Name}"/>

        <Label Content="Pack Size:"/>
        <Label Content="{Binding Size}" ToolTip="Archived size of all files."/>

        <Label Content="Unpack Size:"/>
        <Label Content="{Binding UnpackSize}" ToolTip="Real size of all files. The amount which files will ocupy after unpack."/>
    </WrapPanel>
</UserControl>
